# MIE1001 Team 5&4 — data acquisition for the portable quadrupole mass spectrometer

Foundation of Imaging Engineering (MIE1001), Maastricht University, 2026.
Team 5 owns the data chain: detector signal → preamp → ADC → Arduino UNO → laptop, plus the DAC that
sends the mass command to the Extrel 150-QC quadrupole controller.

```
Faraday cup ─► preamp (LMP7721, 100 MΩ) ─► ADS1115 16-bit ADC ─I²C─► Arduino UNO ─USB─► laptop
                                               MCP4725 12-bit DAC ◄─I²C─┘ ─► mass command (0-5 V now; Extrel wants 0-10 V)
```

## Contents

| Folder | What |
|---|---|
| `firmware/` | Arduino sketches (open in the Arduino IDE; board: Arduino Uno) |
| `hardware/mie1001_daq_simple/` | **Simple shield, rev E**: plug-in ADS1115 + MCP4725 breakouts, screw terminals, and the Faraday-cup preamp. Ordered from Digi-Key and hand-soldered. See its README |
| `hardware/mie1001_daq_board/` | **Full shield, rev E**: everything soldered (ADS1115, MCP4725, preamp), BNC input, CEM/Faraday ranges |
| `hardware/enclosure/` | First idea for a 3D-printed enclosure (transparent PETG, OpenSCAD source, STLs, renders). Not printed, not a team decision yet |
| `data/` | Real measurements (`2026-09-20_selftest/`: first self-tests of the ADS1115 and MCP4725 on the Uno, with plots) and clearly labelled simulations (`simulations/`) |

### Firmware

| Sketch | Tests | For | Serial |
|---|---|---|---|
| `DAQ_shield_scan` | **Mass scan**: DAC code sweep on QPS OUT, preamp current (A1−A3) with 50 Hz window averaging, A2 command read-back (JP1), zero/offset, trigger on D8, self-check (`i`) | **shield** (rev E) | 115200 |
| `DAQ_loopback_test` | DAC → ADC loopback (wire QPS OUT to SIGNAL IN) | **shield** | 115200 |
| `DAQ_mini_chain` | Chain: DAC → UNO A0 → PWM D9 → RC filter → ADS1115. Assumes ADS A1 = DAC; on the shield A1 is PRE_OUT | **breadboard only** | 115200 |
| `ADS1115_selftest` | ADS1115 alone: reads the UNO's 3.3 V, GND and 5 V pins | breadboard | 9600 |
| `MCP4725_standalone_test` | MCP4725 alone, read back on the UNO's A0; scans the I²C bus if not found | breadboard | 9600 |
| `MCP4725_sine_scope` | MCP4725 sine wave, sent vs. read on the Serial Plotter | breadboard | 115200 |
| `SparkFun_MCP4725_waveform` | SparkFun's 512-point sine example (J. Bartlett, 2014, beerware). Its A2/A3 power trick is for a bare breakout on the UNO header, not the shield | breadboard (shield: drop the A2/A3 lines) | — |
| `ioannis_DAC_3V` | Ioannis's sketch: holds the MCP4725 (0x60) at 3 V. Now measures the real 5 V rail first (internal bandgap), since a fixed code 2457 gave ~2.9 V on USB | either | 115200 |
| `PwmGenerator` | Two-channel PWM generator on pins 9/10 (bench tool, not DAQ) | bench | 115200 |

Libraries: Adafruit ADS1X15, Adafruit MCP4725. I²C addresses: ADS1115 0x48, MCP4725 0x60.

### Hardware

Both boards are **generated by Python** (`generate_schematic.py`, `generate_pcb.py`); `./build.sh` rebuilds the
KiCad files, runs ERC, DRC (with schematic parity) and the preamp guard check, and writes the fab files in
`fab/` (gerbers, Digi-Key BOM; the JLCPCB files are optional leftovers). Edits made in the KiCad GUI are overwritten by the next build. Needs KiCad 10 (flatpak `org.kicad.KiCad`).
The simple board's PCB script reuses the router in `../mie1001_daq_board/`, so keep both folders side by side.

## Status (2026-09-25)

- Firmware: all sketches compile for the UNO. The self-tests pass on the real Uno (see `data/`);
  `DAQ_shield_scan` is untested until the shield is soldered.
- Simple shield rev E: fixes from an independent design review (closed guard ring under the LMP7721, mask-free
  guard, 100k/100k VREF divider, A4/A5 wired, JP1 mass-command read-back). ERC, DRC and guard check clean.
  Not yet ordered. Everything is ordered from Digi-Key and soldered by hand. Do a 1:1 paper print check on a real UNO first.
- Open: the Extrel 150-QC mass command is 0-10 V (a x2 stage with its own supply is still needed) and its
  volts-per-amu scaling is unconfirmed.
